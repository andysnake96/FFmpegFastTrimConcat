ora ci sono 2 serializzazioni:	obj <->json mediante __dict__
				GUI obj->tupleList->json 
meglio migrare alla prima, con supporto a estensione selezione.
	e.g.: parto da scansione, sovrascrivo elementi GIA PRESENTI(del obsoleti) con prev dump

concat.list + time cuts  in concat demuxer syntax --> Hope reverse ! :) XD
README.md
