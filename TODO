#TOP
-button hidden re grid()
-test GUI with whole new functionalities + debug

#nn essenziali
-move to MMSys dump items scanned logic     wrapper semplici
    TEST: collection move path, scannedDump(con cuts embedded) reupdate

#perfezionamenti utili ma ora nn utili
-concat.list + time cuts  in concat demuxer syntax --> Hope reverse ! :) XD
-doc convergence
-README.md
